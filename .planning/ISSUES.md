# Issues

Deferred enhancements and concerns discovered during planning or execution.

## Open

- [ ] Update `package.json` description so it matches the current pan-Mandarin 10k CI engine direction instead of the older HSK-only MVP wording.
- [ ] Decide whether `web-dist/` should be tracked, ignored, or treated as disposable build output after Git state is fully established.
- [ ] Decide whether `.planning/PAN_MANDARIN_VOCAB_PLAN.md` should be folded into Phase 2 context, archived as prior research, or kept as a standalone plan note.

## Closed

- [x] Add a short generated baseline report after Phase 1 verification with current counts from `src/index.ts`.
